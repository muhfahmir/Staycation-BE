<%- include('../../../partials/header.ejs') %>

<!-- Page Wrapper -->
<div id="wrapper">

    <!-- Sidebar -->
    <%- include('../../../partials/sidebar.ejs') %>
    <!-- End of Sidebar -->

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">

        <!-- Main Content -->
        <div id="content">

            <!-- Topbar -->
            <%- include('../../../partials/navbar.ejs') %>
            <!-- End of Topbar -->

            <!-- Begin Page Content -->
            <div class="container-fluid">
                <%- include('../../../partials/message.ejs') %>

                <!-- Page Heading -->
                <h1 class="h3 mb-4 text-gray-800">Detail Item</h1>
                <!-- End Heading -->

                <ul class="nav nav-tabs" id="myTab" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" id="showFeature-tab" data-toggle="tab" href="#showFeature" role="tab"
                            aria-controls="showFeature" aria-selected="true">Show Feature</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" id="showActivity-tab" data-toggle="tab" href="#showActivity" role="tab"
                            aria-controls="showActivity" aria-selected="false">Show Activity</a>
                    </li>

                </ul>
                <div class="tab-content" id="myTabContent">
                    <%- include('show_feature.ejs') %>
                    <%- include('show_activity.ejs')  %>

                </div>





            </div>
            <!-- /.container-fluid -->

        </div>
        <!-- End of Main Content -->

        <!-- Footer -->
        <%- include('../../../partials/footer.ejs') %>
        <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->

</div>
<!-- End of Page Wrapper -->

<!-- Scroll to Top Button-->
<a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
</a>

<%- include('../../../partials/modal_logout.ejs') %>
<%- include('show_modal_activity.ejs') %>
<%- include('show_modal_feature.ejs') %>
<%- include('../../../partials/js.ejs') %>
<script>
    $(document).ready(function () {
        $("#dataTableActivity").DataTable();
    })
    // CKEDITOR.replace('description');
    $('#dataTableActivity').on('click', '.button-update', function () {
        let id = $(this).data('id');
        let name = $(this).data('name')
        let type = $(this).data('type');
        $('#editModalActivity').modal('show');
        $('.id').val(id);
        $('.name').val(name);
        $('.type').val(type);
    })

    $('#dataTable').on('click', '.button-update', function () {
        let id = $(this).data('id');
        let name = $(this).data('name')
        let qty = $(this).data('qty')
        // console.log(id + name + nameBank + nomorRekening)
        $('#editModalFeature').modal('show');

        // ngirim melalui kelas
        $('.id').val(id);
        $('.name').val(name)
        $('.qty').val(qty)
    })
</script>